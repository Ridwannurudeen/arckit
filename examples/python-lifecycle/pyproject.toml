[project]
name = "python-lifecycle"
version = "0.0.1"
description = "Full ERC-8183 lifecycle on Arc testnet using the arckit Python SDK"
requires-python = ">=3.10"
dependencies = [
    "arckit",
    "python-dotenv>=1.0.0",
]
