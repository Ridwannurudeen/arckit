# Arc testnet private key — fund it via the Arc faucet first.
# WARNING: never commit a real private key.
PRIVATE_KEY=0x

# Optional: override the testnet RPC.
# RPC_URL=https://rpc.testnet.arc.network
